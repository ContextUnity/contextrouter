# ContextRouter env template (copy to `.env` in your working directory)
#
# Loaded automatically by the CLI via python-dotenv.
#
# Core runtime config (contextrouter core):
CONTEXTROUTER_CORE_CONFIG_PATH=/abs/path/to/core/settings.toml
#
# Ingestion pipeline config (modules/ingestion/rag):
CONTEXTROUTER_CONFIG_PATH=/abs/path/to/ingestion/settings.toml
# Alternatively:
# CONTEXTROUTER_ASSETS_PATH=/abs/path/to/assets
#
# Google / Vertex credentials:
GOOGLE_APPLICATION_CREDENTIALS=/abs/path/to/credentials.json
#
# Vertex AI mode note:
# ContextRouter sets `GOOGLE_GENAI_USE_VERTEXAI=true` by default to avoid the Google GenAI SDK
# accidentally trying API-key auth. If you need to override this behavior, export
# `GOOGLE_GENAI_USE_VERTEXAI=false` before importing/starting ContextRouter.
#
# Langfuse observability (optional):
LANGFUSE_PUBLIC_KEY=pk_...
LANGFUSE_SECRET_KEY=sk_...
LANGFUSE_HOST=https://cloud.langfuse.com
# LANGFUSE_ENVIRONMENT=develop
# LANGFUSE_SERVICE_NAME=contextrouter
